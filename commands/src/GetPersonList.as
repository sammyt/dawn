package
{
	import uk.co.ziazoo.notifier.AsyncCallback;
	
	public class GetPersonList extends AsyncCallback
	{
		public function GetPersonList( result:Function = null, fault:Function = null )
		{
			super( result, fault );
		}
	}
}